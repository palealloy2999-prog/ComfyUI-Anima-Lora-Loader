[project]
name = "comfyui-anima-lora-loader"
version = "0.1.0"
description = "ANIMA LoRA loader using LoRA Manager widgets and in-memory block remapping"
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
